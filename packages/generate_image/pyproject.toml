[project]
name = "generate-image"
version = "0.1.0"
description = "Lambda that generates images via Bedrock (Stability AI) and writes them to S3."
requires-python = ">=3.12.10"
dependencies = [
    "boto3>=1.43",
]

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
