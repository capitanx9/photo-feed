meta {
  name: remove-item
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/cart/items/{{cartItemId}}/
  body: none
  auth: none
}

script:post-response {
  if (res.status === 204) {
    bru.setEnvVar("cartItemId", "");
  }
}
