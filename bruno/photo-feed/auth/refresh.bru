meta {
  name: refresh
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/refresh/
  body: none
  auth: none
}

docs {
  Rotates the refresh cookie. Reads the current refresh_token cookie set by
  /login, blacklists it, issues a fresh pair as cookies.
}
