# Production image tags. SOURCE OF TRUTH for what runs on EC2.
#
# This file is rewritten by GitHub Actions every time cd-api or cd-web
# builds + pushes a new image. The rewrite is committed back to main as
# a regular commit, which then triggers the deploy-prod workflow.
#
# Don't edit by hand unless rolling back — use `git revert` on the CI
# commit that introduced the bad tag, that's the cleanest rollback.

API_IMAGE_TAG=0353bc362b78
WEB_IMAGE_TAG=cb2491b72c35
